# FLEET CONFIG =================================================================
# RMF Fleet parameters

rmf_fleet:
  name: "DeliveryRobot"
  fleet_manager:
    ip: "127.0.0.1"
    port: 22012
    user: "some_user"
    password: "some_password"
  limits:
    linear: [0.6, 0.8] # velocity, acceleration
    angular: [0.6, 1.0] # velocity, acceleration
  profile: # Robot profile is modelled as a circle
    footprint: 0.25 # radius in m
    vicinity: 0.35 # radius in m
  reversible: True # whether robots in this fleet can reverse
  battery_system:
    voltage: 24.0 # V
    capacity: 40.0 # Ahr
    charging_current: 10.0 # A
  mechanical_system:
    mass: 40.0 # kg
    moment_of_inertia: 20.0 #kgm^2
    friction_coefficient: 0.22
  ambient_system:
    power: 20.0 # W
  tool_system:
    power: 0.0 # W
  recharge_threshold: 0.10 # Battery level below which robots in this fleet will not operate
  recharge_soc: 1.0 # Battery level to which robots in this fleet should be charged up to during recharging tasks
  publish_fleet_state: 15.0 # Publish frequency for fleet state, ensure that it is same as robot_state_update_frequency
  account_for_battery_drain: True
  lane_merge_distance: 0.12
  task_capabilities: # Specify the types of RMF Tasks that robots in this fleet are capable of performing
    loop: False
    delivery: True
    clean: False
    finishing_request: "charge" # [park, charge, nothing]

# TinyRobot CONFIG =================================================================

robots:
  # Here the user is expected to append the configuration for each robot in the
  # fleet.
  # Configuration for TinyRobot1
  DeliveryRobot1:
    address: "DeliveryRobot1"
    robot_config:
      max_delay: 4.0 # Tuned for faster yet stable multi-fleet replanning
      filter_waypoints: False
    rmf_config:
      robot_state_update_frequency: 15.0
      start:
        map_name: "L1"
        waypoint: "DeliveryRobot_charger"
        orientation: 0.0
      charger:
        waypoint: "DeliveryRobot_charger"


